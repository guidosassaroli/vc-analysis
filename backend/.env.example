ANTHROPIC_API_KEY=your_api_key_here
# Optional: higher rate limits and authenticated GitHub Search
GITHUB_TOKEN=
# Optional: Supabase Auth (leave blank for local dev — uses "dev_user")
SUPABASE_JWT_SECRET=
